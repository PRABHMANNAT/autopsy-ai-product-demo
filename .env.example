# Pinch payments — fill these in when you wire up billing.
# Copy this file to .env.local (git-ignored) and add your real keys.
PINCH_SECRET_KEY=
PINCH_WEBHOOK_SECRET=
PINCH_API_BASE=https://api.getpinch.com.au/v1

# Used to build redirect URLs back into the app.
NEXT_PUBLIC_APP_URL=http://localhost:3000
